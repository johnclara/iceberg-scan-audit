apply from: "${project.rootDir}/gradle/customBuildscripts/java.gradle"

dependencies {
    api(project(":mock-catalog"))
    api("org.apache.iceberg:iceberg-data")
    api("org.apache.iceberg:iceberg-parquet")
    api("org.apache.iceberg:iceberg-orc")
}
